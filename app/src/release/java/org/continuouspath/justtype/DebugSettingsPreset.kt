package org.continuouspath.justtype

import android.content.Context

/**
 * Release build stub — no-op. The debug source set contains the real implementation.
 */
object DebugSettingsPreset {
	fun apply(context: Context) { /* no-op in release builds */ }
}
